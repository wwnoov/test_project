<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Student">
    <insert id="save" parameterType="student">
        insert into student(studentId, studentName, korean, english, math)
        values(#{studentId}, #{studentName}, #{korean}, #{english},#{math})
    </insert>

    <select id="login" parameterType="student" resultType="student">
        select * from student where studentId =#{studentId} and password = #{password}
    </select>

    <select id="findAll" resultType="student">
        select * from student
    </select>
    <select id="findById" parameterType="string" resultType="student">
        select * from student where studentId =#{studentId}
    </select>

    <update id="update" parameterType="student">
        update student set korean = #{korean} , english =#{english} , math=#{math} where studentId = #{studentId}

    </update>
    <delete id="delete" parameterType="String">
        delete from student where studentId = #{studentId}
    </delete>
</mapper>
